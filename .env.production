# Production Environment Configuration
# CRITICAL: Set these before deploying to production

# Database Configuration
NEO4J_PASSWORD=change_this_secure_password_123!
NEO4J_USER=neo4j

# JWT Configuration (use a cryptographically secure key)
JWT_SECRET=change_this_super_secure_jwt_secret_key_at_least_32_characters_long_123456789

# Environment
ENVIRONMENT=production

# API Configuration (update for your production domain)
API_CORS_ORIGINS=https://your-production-domain.com,https://api.your-domain.com